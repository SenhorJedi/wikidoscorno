import 'package:flutter/material.dart';

class InfiniteScrollerArtigo extends StatelessWidget {
  const InfiniteScrollerArtigo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
